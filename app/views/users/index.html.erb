<p style="color: green"><%= notice %></p>

<h1><%= t('users') %></h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to t('show_this_user'), user %>
    </p>
  <% end %>
</div>

<%= link_to t('new_user'), new_user_path %>
